__pycache__/
*.pyc
*.json
*.json.sha256

# The fold fixture is derived from a real campaign store with every identifier replaced,
# and it is the only input that exercises a four-row inbound exchange. Ignoring it made
# tests/test-fold.sh unrunnable from a clean checkout, which defeats an independent review.
!tests/fixtures/audit-events-from-campaign.json
